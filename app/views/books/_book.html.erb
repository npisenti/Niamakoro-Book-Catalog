<tr>
  <td><%= book.collection %></td>
  <td width="50%"><%= link_to book.title, book_path(book) %> 
    <% if current_user %>
      <!--      | <span class="label warning"><%= link_to "Edit", edit_book_path(book.id) %></span> -->
    <% end %>
    </td>
  <td><% book.subjects.each do |s| %><%= "#{s.name}" %><%= ", " unless s == book.subjects.last %><% end %></td>
</tr>
